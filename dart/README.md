# Batman Dart Legacy Implementation

This directory contains the previous Dart implementation of Batman. It is kept
as legacy/reference material while the Rust implementation at the repository
root is the first-class implementation.

For current build, install, baseline, integrity, accept, exclusions, and release
workflows, use the root Rust project.
